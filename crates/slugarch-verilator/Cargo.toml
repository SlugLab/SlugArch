[package]
name = "slugarch-verilator"
version = "0.1.0"
edition.workspace = true
license.workspace = true

[dependencies]
slugarch-verilator-sys = { path = "../slugarch-verilator-sys" }
slugarch-ir = { path = "../slugarch-ir" }
