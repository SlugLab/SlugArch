[package]
name = "slugarch-verilator-sys"
version = "0.1.0"
edition.workspace = true
license.workspace = true
build = "build.rs"

[build-dependencies]
bindgen.workspace = true
cc.workspace = true

[dependencies]
# Raw FFI only. Safe wrappers live in slugarch-verilator.
