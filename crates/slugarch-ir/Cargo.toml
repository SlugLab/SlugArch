[package]
name = "slugarch-ir"
version = "0.1.0"
edition.workspace = true
license.workspace = true

# Minimal stub to satisfy workspace manifest loading while Task 2 verifies
# the vendored ptx_parser builds. Task 5 overwrites this file with the real
# slugarch-ir crate manifest.
